---
defaultNamespace: velero
labels:
  bundle: &name velero-conf
helm:
  releaseName: *name
  atomic: true
dependsOn:
  - selector:
      matchLabels:
        bundle: external-secrets-conf
targetCustomizations:
  - name: dev
    clusterSelector:
      matchLabels:
        site: dev
    yaml:
      overlays:
        - dev
