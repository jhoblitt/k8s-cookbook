---
defaultNamespace: &name rook-ceph
labels:
  bundle: *name
helm:
  chart: *name
  releaseName: *name
  repo: https://charts.rook.io/release
  version: v1.11.2
  valuesFiles:
    - values.yaml
  timeoutSeconds: 1800
  atomic: true
